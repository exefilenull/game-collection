# Implementation Plan

- [ ] 1. 対象レコード抽出スクリプトの実装
- [x] 1.1 (P) 対象抽出スクリプトの実装
  - 全カテゴリの`data/*.json`を読み取り、プレイ状況が「未プレイ」以外(未設定を含めて除外)かつ`walkthrough_note_path`が未設定のレコードのみを対象として列挙する
  - `walkthrough_note_path`が既に設定されているレコードと、プレイ状況が「未プレイ」または未設定のレコードは対象から除外する
  - `soft_title`キーが欠落した不正なレコードに遭遇してもクラッシュせず、警告を出しつつスキップする
  - 対象レコード1件につき1行のJSON(`file`/`hardware`/`soft_title`)を標準出力へ出力する
  - 観測可能な完了条件: リポジトリの実データに対して実行し、出力された対象レコード数と手動で数えたサンプル件数が一致する
  - _Requirements: 1.1, 1.2, 1.3, 1.4_
  - _Boundary: find_missing_walkthrough_notes.py_

- [x] 1.2 対象抽出スクリプトの動作検証
  - プレイ状況が「未プレイ」「未設定」「プレイ中」「全クリア」の4パターンで、対象への含有・除外が要件どおりになることを確認する
  - `walkthrough_note_path`が既に設定されているレコードが対象から除外されることを確認する
  - `soft_title`が欠落したレコードを含む入力でクラッシュせずスキップされることを確認する
  - 観測可能な完了条件: 上記全パターンの検証結果(pass/fail)が記録され、いずれもpassしている
  - _Requirements: 1.2, 1.3, 1.4_
  - _Boundary: find_missing_walkthrough_notes.py_

- [ ] 2. 紐付けスクリプトの実装
- [x] 2.1 (P) 紐付けスクリプトの実装
  - 対象ファイル・ハード名・ゲームタイトル(完全一致)・書き込むパスの4つを引数として受け取り、該当レコード1件の`walkthrough_note_path`を更新する
  - 該当レコードに既に`walkthrough_note_path`が設定されている場合は上書きせずスキップし、その旨を報告する(エラーにしない)
  - 該当レコードが0件または2件以上見つかった場合は、何も書き込まずにエラーを報告する
  - 書き込みは既存ファイルのフォーマット(UTF-8, 非ASCIIエスケープ無し, インデント2スペース, LF)を保ったまま行う
  - 観測可能な完了条件: 既存29件のいずれか1件を模したテストデータに対して実行し、対象レコードのJSON内に`walkthrough_note_path`が書き込まれ、他のレコード・他のフィールドの値が一切変化していないことをdiffで確認できる
  - _Requirements: 3.2, 3.3_
  - _Boundary: link_walkthrough_note.py_

- [x] 2.2 紐付けスクリプトの動作検証
  - 該当レコードが0件・1件・2件以上の場合それぞれで期待どおりの挙動(エラー/成功/エラー)になることを確認する
  - `walkthrough_note_path`が既に設定済みのレコードに対してスキップ(上書きしない)されることを確認する
  - 書き込み後のJSONファイルが既存フォーマット(UTF-8, 非ASCIIエスケープ無し, インデント2スペース)を保っていることをdiffで確認する
  - 観測可能な完了条件: 上記全パターンの検証結果(pass/fail)が記録され、いずれもpassしている
  - _Requirements: 3.2, 3.3_
  - _Boundary: link_walkthrough_note.py_

- [ ] 3. 生成手順書(SKILL.md)の作成
- [ ] 3.1 HTML構成・文体・カラーコード規則セクションの作成
  - `game_notes/`配下の既存29件を参照し、タイトル見出し・目次・折りたたみ式セクションから成るHTML構成を明文化する
  - 改行は明示的な`<br>`タグでのみ表現し`white-space: pre-line`を使わないこと、見出しレベル別の区切り改行数(見出し大=3・見出し中=2・見出し小以下=1)を明文化する
  - タイトルに色指定がある場合はタイトル全体を色付けし、無い場合は既定で黒(`#000000`)とする規則を明文化する
  - 既存メモの文体(個人の感想を含むトーン)を模倣する指針を記述する
  - 特定のAIプロバイダのAPIやツールに依存しない、プロバイダ非依存の前提で記述する
  - 観測可能な完了条件: `game_notes/SKILL.md`にHTML構成・改行規則・カラーコード規則・文体模倣指針のセクションが存在する
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 4.1, 4.4_
  - _Boundary: game_notes/SKILL.md_

- [ ] 3.2 対象抽出・配置紐付け手順セクションの作成
  - `find_missing_walkthrough_notes.py`の実行方法と出力(JSON Lines)の読み方を説明する
  - 生成先のディレクトリ・ファイルパス規則(`game_notes/{category}/{hardware}/{title}.html`)を説明する
  - `link_walkthrough_note.py`の実行方法(4引数)と、既に紐付け済みのレコードはスキップされる(生成処理をしない)ことを説明する
  - 観測可能な完了条件: `game_notes/SKILL.md`に対象抽出コマンド例・配置パス規則・紐付けコマンド例が具体的な実行例つきで記載されている
  - _Requirements: 3.1, 3.4, 4.2, 4.3_
  - _Depends: 1.1, 2.1_
  - _Boundary: game_notes/SKILL.md_

- [ ] 4. 統合検証
- [ ] 4.1 抽出から再抽出までの一連の流れの検証
  - 実データのコピー上で、対象抽出→(手動で1件HTML作成)→紐付け→再抽出、という一連の流れを実行する
  - 紐付け後のレコードが再抽出結果の一覧から消えることを確認する
  - 観測可能な完了条件: 紐付け前後で対象抽出スクリプトの出力件数が1件減っていることが確認できる
  - _Requirements: 3.4_
  - _Depends: 1.1, 2.1_
  - _Boundary: find_missing_walkthrough_notes.py, link_walkthrough_note.py_

- [ ] 4.2 SKILL.md単独での試験生成レビュー
  - `game_notes/SKILL.md`の指示のみに従うAIエージェントの視点で、対象レコード1件を試験的に生成する
  - 生成物が既存29件と構造的に一致すること(見出し・目次・折りたたみ・`<br>`方式・見出しレベル別の区切り改行数・タイトル色付け)を確認する
  - 観測可能な完了条件: 試験生成したHTMLと既存実例とのチェックリスト対照結果(項目ごとのpass/fail)が記録され、いずれもpassしている
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 4.1_
  - _Depends: 3.1, 3.2_

## Implementation Notes
- タスク1.1の実装者が、実装と同時にTDD(RED→GREEN)で1.2の受け入れ基準(未プレイ/未設定/プレイ中/全クリアの各パターン、既紐付け除外、soft_title欠落のスキップ)を網羅する単体テスト11件を作成・全pass済みだったため、1.2は追加実装なしでそのままレビュー・完了とした(親コントローラーが`python -m unittest tools/test_find_missing_walkthrough_notes.py -v`を再実行し11件全pass・境界内であることをフレッシュな証拠として確認済み)。
- タスク2.1の実装時のテストスイート(6件、0/1/2+件マッチ・既設定スキップ・CRLF/LF双方のフォーマット保持を網羅)がそのまま2.2の受け入れ基準を満たしていたため、2.2も追加実装なしで完了とした(ラウンド3レビュアーがミューテーションテスト含め独立に再検証済み)。
- タスク2.1(`link_walkthrough_note.py`)の実装過程で、`data/*.json`12ファイルのうち10ファイルがCRLF・2ファイル(`data/1_nintendo.json`, `data/8_sie.json`)のみLFであり、かつ10ファイルは末尾改行が無いことが判明した。design.mdのBatch/Job Contractの記述は「LF」を前提としているが、実データの実態と異なるため、`link_walkthrough_note.py`は元ファイルの生バイトから改行コード・末尾改行有無を判定して完全に復元する実装とした。SKILL.md執筆時(タスク3.x)にこの実態を踏まえる必要はない(SKILL.mdはHTML生成規則の文書であり、JSONの改行コードには言及しないため)が、design.mdの当該記述はいずれ実態に合わせて更新すべき申し送り事項として記録する。
  - _Boundary: game_notes/SKILL.md_
